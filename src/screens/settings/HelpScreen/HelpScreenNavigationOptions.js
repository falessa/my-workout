const navigationOptions = (helpScreenTitle) => {
    // TODO: check comment about hooks in SettingsNavigator
    // const { t } = useTranslation()

    return {
        headerTitle: helpScreenTitle
    }
}

export default navigationOptions