const navigationOptions = (aboutUsScreenTitle) => {
    // TODO: check comment about hooks in SettingsNavigator
    // const { t } = useTranslation()

    return {
        headerTitle: aboutUsScreenTitle
    }
}

export default navigationOptions